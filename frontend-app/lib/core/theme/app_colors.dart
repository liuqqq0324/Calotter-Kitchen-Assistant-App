import 'package:flutter/material.dart';

// 应用颜色常量
// 可以从主题配置中提取颜色常量到这里

class AppColors {
  // 可以从 ThemeData 中提取的颜色
  static const Color primarySeed = Colors.deepOrange;
  
  // 可以添加更多颜色常量
  // static const Color secondaryColor = Color(0xFF...);
}

