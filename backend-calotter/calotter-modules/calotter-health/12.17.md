完成：calotter-health 模块已改为使用 JPA
完成的工作
1. 更新 NutritionLogRepository
添加了按日期和来源类型查询的方法
添加了权限校验查询方法（findByIdAndFamilyMember）
添加了营养汇总查询方法（sumNutritionByDateRange）
2. 重写 IntakeServiceImpl（使用 JPA）
移除了所有 MyBatis Mapper 引用
使用 NutritionLogRepository 和 FamilyMemberRepository 进行数据访问
实现了所有接口方法：
getTodayIntakes() - 查询今日摄入记录
updateIntakePercentage() - 更新摄入百分比（当前仅记录警告，因为 NutritionLog 不支持该字段）
addManualIntake() - 添加手动摄入（使用 AI 估算营养值）
deleteIntake() - 删除摄入记录
添加了实体转换方法（NutritionLog → DTO）
集成了 ManualNutritionEstimator（AI 营养估算）
3. 重写 NutritionServiceImpl（使用 JPA）
移除了所有 MyBatis Mapper 引用
使用 HealthGoalRepository、FamilyMemberRepository、NutritionLogRepository 进行数据访问
实现了所有接口方法：
getWeeklyNutritionTargets() - 获取周营养目标（从 HealthGoal 计算）
getWeeklyNutritionSummary() - 获取周营养摘要（从 NutritionLog 汇总）
使用 HealthGoal 实体获取营养目标，不再需要单独的 NutritionTarget 实体
主要变更
数据访问层
从 MyBatis Mapper → JPA Repository
使用 Spring Data JPA 的查询方法
实体映射
NutritionLog 对应旧版的 IntakeRecord
HealthGoal 用于营养目标（不再需要 NutritionTarget）
字段映射
NutritionLog.calories → DTO energy
NutritionLog.carb → DTO carbohydrates
实体层存储实际摄入值，DTO 层使用统一字段名
注意事项
NutritionLog 当前不支持 consumedPercentage 字段
所有记录假设为 100% 摄入
如需支持百分比调整，后续可在 NutritionLog 中添加该字段
编译状态
BUILD SUCCESS
所有文件编译通过
无编译错误
后续建议
如需支持 consumedPercentage：
在 NutritionLog 实体中添加 consumedPercentage 字段
更新 updateIntakePercentage() 方法的实现
性能优化：
考虑使用 DailyNutrientAggregate 进行周汇总查询（已存在）
当前使用 NutritionLog 直接汇总，数据量大时可能较慢
所有文件已成功改为使用 JPA，编译通过。


替换完成总结
后端文件（已全部替换）
已将所有 familyMemberId 替换为 userId：
Controller 层 (2个文件)
NutritionController.java - 所有参数和方法
IntakeController.java - 所有参数和方法
Service 接口层 (2个文件)
INutritionService.java - 所有方法签名和注释
IIntakeService.java - 所有方法签名和注释
Service 实现层 (2个文件)
NutritionServiceImpl.java - 所有方法实现
IntakeServiceImpl.java - 所有方法实现
DTO 层 (1个文件)
ManualNutritionLogRequest.java - 字段名从 familyMemberId 改为 userId
前端文件
/Users/qliu/A-team-PersonalSousChef/frontend-app/lib/pages/home 目录下的文件：
home_page.dart - 未使用 familyMemberId 或 userId
add_food_dialog.dart - 未使用 familyMemberId 或 userId
todays_recipes_dialog.dart - 未使用 familyMemberId 或 userId
这些文件通过 HomepageApiService 调用 API，不直接使用这些参数。
编译验证
BUILD SUCCESS
所有文件编译通过
无编译错误
替换统计
后端：7 个文件，55 处替换
前端：0 个文件需要修改（home 目录下无相关使用）
所有替换已完成，代码已编译通过。

已完成的修改
NutritionLog 实体类
calories → energy (字段名和数据库列名)
carb → carbohydrates (字段名和数据库列名)
DailyNutrientAggregate 实体类
totalCalories → totalEnergy
totalCarb → totalCarbohydrates
goalCaloriesSnapshot → goalEnergySnapshot
goalCarbSnapshot → goalCarbohydratesSnapshot
Repository 查询
NutritionLogRepository.sumNutritionByDateRange() SQL 查询中的字段名已更新
Service 实现类
IntakeServiceImpl - 所有字段引用已更新
NutritionServiceImpl - 已检查，无需修改（使用 DTO 字段）
NutritionLogService - 所有字段引用已更新
NutritionAggregateService - 所有字段引用已更新
编译状态
编译成功：所有文件已通过编译验证
数据库迁移
由于使用了 JPA 的 ddl-auto: update，数据库表结构会在应用启动时自动更新：
nutrition_logs 表的 calories 列将重命名为 energy
nutrition_logs 表的 carb 列将重命名为 carbohydrates
daily_nutrient_aggregates 表的相应列也会更新
注意：如果数据库中已有数据，建议先备份，或编写数据库迁移脚本手动重命名列。
所有字段命名已统一为 energy 和 carbohydrates。

编译通过。所有与 FamilyMember 相关的字段和方法已改为 user。
修改总结
已完成的修改
实体类
NutritionLog: FamilyMember familyMember → User user
DailyNutrientAggregate: FamilyMember familyMember → User user
数据库列名映射: family_member_id → user_id
索引名称更新: idx_log_member_date → idx_log_user_date, idx_aggregate_member_date → idx_aggregate_user_date
Repository 接口
NutritionLogRepository: 所有方法名从 findByFamilyMember* 改为 findByUser*
DailyNutrientAggregateRepository: 所有方法名从 findByFamilyMember* 改为 findByUser*
JPQL 查询: log.familyMember → log.user
Service 实现类
IntakeServiceImpl: FamilyMemberRepository → UserRepository, 所有变量和方法调用已更新
NutritionServiceImpl: 添加 UserRepository, 通过 findFamilyMemberByUser() 辅助方法查询 HealthGoal
NutritionLogService: 添加 UserRepository, 事件处理中通过 FamilyMember.user 获取 User
NutritionAggregateService: 添加 UserRepository, 所有方法参数和调用已更新
辅助方法
添加 findFamilyMemberByUser() 方法用于通过 User 查找对应的 FamilyMember（用于查询 HealthGoal）
注意事项
HealthGoal 仍使用 FamilyMember：由于 HealthGoal 在 calotter-user 模块中且仍关联 FamilyMember，代码中通过 User 查找对应的 FamilyMember 后再查询 HealthGoal。
事件兼容性：CookingSessionCompletedEvent 仍使用 FamilyMemberId，代码中先查找 FamilyMember，再通过 member.getUser() 获取 User。
数据库迁移：由于使用了 JPA 的 ddl-auto: update，数据库表结构会在应用启动时自动更新：
nutrition_logs.family_member_id → user_id
daily_nutrient_aggregates.family_member_id → user_id
索引名称也会相应更新
编译状态
编译成功：所有文件已通过编译验证
所有 FamilyMember 相关的字段和方法已改为 user。

从 calotter-inventory 模块获取当天做好的菜信息。
修改总结
主要修改
添加 Repository 查询方法
在 LeftoverDishRepository 中添加 findByHouseholdIdAndProducedTimeBetween() 方法，用于查询指定日期范围内的剩菜
修改 IntakeServiceImpl.getTodayIntakes()
当 source = "recipe" 时，调用 getTodayRecipesFromInventory() 从 inventory 模块获取数据
优先从 CookingSession 获取当天完成的烹饪会话（完整菜品）
如果没有，则从 LeftoverDish 获取当天产生的剩菜（备选）
新增转换方法
convertCookingSessionToIntakeItem(): 将 CookingSession 转换为 IntakeItem
convertLeftoverToIntakeItem(): 将 LeftoverDish 转换为 IntakeItem（备选）
实现逻辑
获取用户的 Household（通过 User -> FamilyMember -> Household）
查询当天完成的 CookingSession（status = COOKED，updateTime 是当天）
如果没有 CookingSession，则查询当天产生的 LeftoverDish（producedTime 是当天）
批量查询对应的 Dish 信息
查找对应的 NutritionLog 以获取 consumedPercentage（如果有）
转换为 IntakeItem 返回
注意事项
优先使用 CookingSession（完整菜品），更准确
如果没有 CookingSession，使用 LeftoverDish（剩菜）作为备选
consumedPercentage 优先从 NutritionLog 获取，如果没有则基于 LeftoverDish 的剩余重量计算
所有修改已通过编译验证。

实现总结
已在 calotter-health 模块实现以下功能：
1. 添加 consumedPercentage 字段支持
在 NutritionLog 实体中添加 consumedPercentage 字段（0.0-100.0）
添加基础营养值字段（baseEnergy, baseProtein, baseFat, baseCarbohydrates, baseFiber）
实际摄入营养值根据 consumedPercentage 动态计算
2. 修改 NutritionLogService.createFromEvent()
存储基础营养值（100%时的值，从 Dish 获取）
设置初始 consumedPercentage（基于事件中的 portionPercentage）
计算实际摄入营养值
3. 修改 IntakeServiceImpl.updateIntakePercentage()
实际更新 consumedPercentage 字段
根据新的 consumedPercentage 重新计算实际摄入营养值
4. 更新转换方法
convertToIntakeItem() 和 convertToUpdateIntakeItem() 使用实际的 consumedPercentage
baseNutrition 使用基础营养值（100%时的值）
effectiveNutrition 使用实际摄入值（已基于 consumedPercentage 计算）
5. 实现定时任务处理剩菜
创建 LeftoverConversionScheduler 定时任务
每天凌晨2点执行，处理前一天未完全消费的菜品
自动将剩余部分转为剩菜存入 inventory 模块
在主应用类添加 @EnableScheduling 启用定时任务
6. 确保 getTodayIntakes() 正确返回当天做的菜
从 NutritionLog 查询，sourceType = APP_COOKING
只显示当天（logDate = today）的 recipes
主要修改的文件：
NutritionLog.java - 添加 consumedPercentage 和基础营养值字段
NutritionLogService.java - 更新创建逻辑，存储基础值和实际值
IntakeServiceImpl.java - 实现 updateIntakePercentage() 和更新转换方法
LeftoverConversionScheduler.java - 新增定时任务处理剩菜
CalotterApplication.java - 添加 @EnableScheduling 启用定时任务
功能流程：
用户完成烹饪会话 → CookingSessionCompletedEvent 发布 → NutritionLogService 创建日志（初始 consumedPercentage 基于 portionPercentage）
用户在 todays_recipes 页面查看当天做的菜 → getTodayIntakes(source='recipe') 返回当天记录
用户调整消费百分比 → updateIntakePercentage() 更新 consumedPercentage 并重新计算营养值
当天过后 → 定时任务自动将未完全消费的菜品转为剩菜存入 inventory
所有功能已实现并通过编译验证。
