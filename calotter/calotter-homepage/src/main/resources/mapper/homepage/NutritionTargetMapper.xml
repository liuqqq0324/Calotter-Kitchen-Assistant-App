<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.calotter.homepage.mapper.NutritionTargetMapper">

    <select id="selectByUserIdAndDate" resultType="com.calotter.homepage.domain.vo.NutritionTargetVo">
        SELECT *
        FROM sous_chef_hp.hp_nutrition_target
        WHERE user_id = #{userId}
          AND week_start &lt;= #{date}
          AND week_end &gt;= #{date}
        LIMIT 1
    </select>

    <select id="selectByUserIdAndWeekStart" resultType="com.calotter.homepage.domain.vo.NutritionTargetVo">
        SELECT *
        FROM sous_chef_hp.hp_nutrition_target
        WHERE user_id = #{userId}
          AND week_start = #{weekStart}
        LIMIT 1
    </select>

</mapper>
