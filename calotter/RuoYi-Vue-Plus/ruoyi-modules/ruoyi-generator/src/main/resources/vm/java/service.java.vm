package ${packageName}.service;

import ${packageName}.domain.vo.${ClassName}Vo;
import ${packageName}.domain.bo.${ClassName}Bo;
#if($table.crud)
import com.calotter.common.mybatis.core.page.TableDataInfo;
import com.calotter.common.mybatis.core.page.PageQuery;
#end

import java.util.Collection;
import java.util.List;

/**
 * ${functionName} service interface
 *
 * @author ${author}
 */
public interface I${ClassName}Service {

    /**
     * Query ${functionName}
     *
     * @param ${pkColumn.javaField} primary key
     * @return ${functionName}
     */
    ${ClassName}Vo queryById(${pkColumn.javaType} ${pkColumn.javaField});

#if($table.crud)
    /**
     * Pagination query ${functionName} list
     *
     * @param bo        query condition
     * @param pageQuery pagination parameters
     * @return ${functionName} paged list
     */
    TableDataInfo<${ClassName}Vo> queryPageList(${ClassName}Bo bo, PageQuery pageQuery);
#end

    /**
     * Query the list of eligible ${functionName} items
     *
     * @param bo query condition
     * @return ${functionName} list
     */
    List<${ClassName}Vo> queryList(${ClassName}Bo bo);

    /**
     * Add ${functionName}
     *
     * @param bo ${functionName}
     * @return if the add operation is successful
     */
    Boolean insertByBo(${ClassName}Bo bo);

    /**
     * Modify ${functionName}
     *
     * @param bo ${functionName}
     * @return if the modification is successful
     */
    Boolean updateByBo(${ClassName}Bo bo);

    /**
     * Verify and batch delete ${functionName} information
     *
     * @param ids     primary keys to be deleted
     * @param isValid whether to conduct a validation of effectiveness
     * @return if the delete operation is successful
     */
    Boolean deleteWithValidByIds(Collection<${pkColumn.javaType}> ids, Boolean isValid);
}
